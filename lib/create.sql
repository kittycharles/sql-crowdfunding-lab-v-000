CREATE TABLE projects (
        id INTEGER PRIMARY KEY,
                title TEXT, 
                funding_goal INTEGER,
                start_date REAL,
                end_date REAL,
            );